# Proxied through Next.js in dev (recommended)
NEXT_PUBLIC_API_URL=/api/backend
BACKEND_URL=http://127.0.0.1:8000
NEXT_PUBLIC_ADMIN_SECRET=dev-secret

# Or call backend directly (requires CORS + both servers running):
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
